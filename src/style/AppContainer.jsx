import styled from '@emotion/styled';

const AppContainer = styled.div({
  margin: '0 auto',
  textAlign: 'center',
  width: '90%',
  '& a': {
    textDecoration: 'none',
    color: 'black',
  },
});

export default AppContainer;
